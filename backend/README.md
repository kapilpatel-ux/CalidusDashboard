# Calidus Dashboard Backend

Node.js + Express + MongoDB + TypeScript backend.

```bash
npm install
npm run dev
```

Required env:

```bash
MONGO_URL=
DB_NAME=calidus_dashboard
CORS_ORIGINS=http://localhost:3000
PORT=8000
```
